#include "../common.h"

class Solution {
public:
    bool isPalindrome(ListNode* head) {
        
    }
};

int main(){

    auto head = GenList(vector<int>{1,2,3,2,1});

    Solution s;
    cout << s.isPalindrome(head) << endl;

}
